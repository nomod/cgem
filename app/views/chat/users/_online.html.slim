- users.each do |user|
  li
    /= link_to user.user_name, chat.conversations_path(user_id: user), remote: true, method: :post
    = link_to user.user_name, "/chat/conversations?user_id=#{user.id}", remote: true, method: :post